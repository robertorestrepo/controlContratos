{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        
        <h2><span class="glyphicon glyphicon-user" aria-hidden="true"></span>Crear Categorias</h2>
        {{ form_start(form) }}
          <div class="form-group">
            {{ form_label(form.nombre) }}
            {{ form_widget(form.nombre) }}
          </div>          
          <div class="form-group">
            {{ form_widget(form.save) }}
          </div>
          
        {{ form_end(form) }}
    </div>
{% endblock %}
